import 'package:flutter/material.dart';

class FormHead extends StatelessWidget {
  const FormHead({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Center(child: Text('Form Registration')),
    );
  }
}
